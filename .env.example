# Copy to .env and adjust if you like. The Makefile, scripts, and Python
# helper all read these values. Defaults match docker-compose.yml.
CLICKHOUSE_HOST=localhost
CLICKHOUSE_HTTP_PORT=8123
CLICKHOUSE_NATIVE_PORT=9000
CLICKHOUSE_DB=learn
CLICKHOUSE_USER=learner
CLICKHOUSE_PASSWORD=learn
